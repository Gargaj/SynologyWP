<Page
    x:Class="SynologyWP.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BlueWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Grid>
    <StackPanel Orientation="Vertical" Width="300" Height="270">
      <TextBlock FontWeight="Bold" FontSize="32" Margin="0,0,0,20" HorizontalAlignment="Center">SynologyWP</TextBlock>
      <TextBlock>NAS URL</TextBlock>
      <TextBox Text="{Binding Host, Mode=TwoWay}" InputScope="Url" PlaceholderText="http://mynas:5051/"/>
      <TextBlock>Username</TextBlock>
      <TextBox Text="{Binding Handle, Mode=TwoWay}" InputScope="Url"/>
      <TextBlock>Password</TextBlock>
      <PasswordBox x:Name="passwordBox"/>
      <Button Click="Login_Click" Margin="0,20,0,0">Log in</Button>
    </StackPanel>
  </Grid>
</Page>

<UserControl
    x:Class="SynologyWP.Inlays.PhotosInlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SynologyWP.Inlays"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:c="using:Microsoft.Xaml.Interactions.Core" 
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
  <Grid>

    <Grid.Resources>
      <CollectionViewSource x:Name="groupedPhotosByMonth" IsSourceGrouped="True"/>
    </Grid.Resources>

    <SemanticZoom IsZoomOutButtonEnabled="True" ScrollViewer.ZoomMode="Enabled">
      <SemanticZoom.ZoomedOutView>
        <GridView x:Name="Months" SelectionMode="None" HorizontalAlignment="Center" Padding="0">
          <GridView.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Group.Key}" Width="96" FontSize="22" Margin="15" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" TextAlignment="Center"/>
            </DataTemplate>
          </GridView.ItemTemplate>
          <GridView.ItemsPanel>
            <ItemsPanelTemplate>
              <VariableSizedWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="3"/>
            </ItemsPanelTemplate>
          </GridView.ItemsPanel>
        </GridView>
      </SemanticZoom.ZoomedOutView>
      <SemanticZoom.ZoomedInView>
        <GridView ItemsSource="{Binding Source={StaticResource groupedPhotosByMonth}}" HorizontalAlignment="Center">

          <GridView.ItemTemplate>
            <DataTemplate>
              <StackPanel Orientation="Vertical" Margin="5,5">
                <Image Width="80" Height="80">
                  <Image.Source>
                    <BitmapImage UriSource="{Binding ImageURL}"/>
                  </Image.Source>
                </Image>
              </StackPanel>
            </DataTemplate>
          </GridView.ItemTemplate>

          <GridView.GroupStyle>
            <GroupStyle HidesIfEmpty="True">
              <GroupStyle.HeaderTemplate>
                <DataTemplate>
                  <Grid Margin="0">
                    <TextBlock Text="{Binding Key}" Foreground="{ThemeResource SystemControlHighlightAccentBrush}"/>
                  </Grid>
                </DataTemplate>
              </GroupStyle.HeaderTemplate>
            </GroupStyle>
          </GridView.GroupStyle>

        </GridView>
      </SemanticZoom.ZoomedInView>
    </SemanticZoom>

  </Grid>
</UserControl>

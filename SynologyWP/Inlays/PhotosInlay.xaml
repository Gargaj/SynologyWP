<UserControl
    x:Class="SynologyWP.Inlays.PhotosInlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SynologyWP.Inlays"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:c="using:Microsoft.Xaml.Interactions.Core" 
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
  <Grid>

    <Grid.Resources>
      <CollectionViewSource x:Name="groupedPhotosByMonth" IsSourceGrouped="True"/>
    </Grid.Resources>

    <SemanticZoom IsZoomOutButtonEnabled="True" ScrollViewer.ZoomMode="Enabled">
      <SemanticZoom.ZoomedOutView>
        <GridView x:Name="Months" SelectionMode="None" HorizontalAlignment="Center" Padding="0">
          <GridView.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Group.Key}" Width="90" FontSize="22" Margin="15" Foreground="{ThemeResource SystemControlHighlightAccentBrush}" TextAlignment="Center"/>
            </DataTemplate>
          </GridView.ItemTemplate>
          <GridView.ItemsPanel>
            <ItemsPanelTemplate>
              <VariableSizedWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="3"/>
            </ItemsPanelTemplate>
          </GridView.ItemsPanel>
        </GridView>
      </SemanticZoom.ZoomedOutView>
      <SemanticZoom.ZoomedInView>
        <GridView ItemsSource="{Binding Source={StaticResource groupedPhotosByMonth}}" HorizontalAlignment="Center">

          <GridView.ItemTemplate>
            <DataTemplate>
              <Canvas Width="70" Height="70">
                <Image Canvas.Top="0" Canvas.Left="0" Width="70" Height="70" Stretch="UniformToFill" Tapped="Image_Tapped">
                  <Image.Source>
                    <BitmapImage UriSource="{Binding ImageURLThumb}"/>
                  </Image.Source>
                </Image>
                <Canvas Canvas.Top="0" Canvas.Left="0" Width="70" Height="70" IsHitTestVisible="False">
                  <i:Interaction.Behaviors>
                    <c:DataTriggerBehavior Binding="{Binding IsVideo}" Value="True">
                      <c:ChangePropertyAction PropertyName="Visibility" Value="Visible" />
                    </c:DataTriggerBehavior>
                    <c:DataTriggerBehavior Binding="{Binding IsVideo}" Value="False">
                      <c:ChangePropertyAction PropertyName="Visibility" Value="Collapsed" />
                    </c:DataTriggerBehavior>
                  </i:Interaction.Behaviors>
                  <FontIcon Canvas.Top="0" Canvas.Left="0" Width="70" Height="70" FontSize="32" Glyph="&#xE768;" Foreground="White"/>
                  <TextBlock Text="{Binding VideoLengthString}" TextAlignment="Right" VerticalAlignment="Bottom" FontSize="10" Canvas.Left="45" Canvas.Top="55" Foreground="White"/>
                </Canvas>
              </Canvas>
            </DataTemplate>
          </GridView.ItemTemplate>

          <GridView.GroupStyle>
            <GroupStyle HidesIfEmpty="True">
              <GroupStyle.HeaderTemplate>
                <DataTemplate>
                  <Grid Margin="0">
                    <TextBlock Text="{Binding Key}" Foreground="{ThemeResource SystemControlHighlightAccentBrush}"/>
                  </Grid>
                </DataTemplate>
              </GroupStyle.HeaderTemplate>
            </GroupStyle>
          </GridView.GroupStyle>

        </GridView>
      </SemanticZoom.ZoomedInView>
    </SemanticZoom>

  </Grid>
</UserControl>
